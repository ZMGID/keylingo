# Implementation

- [ ] Create and validate `src-tauri/icons/AppIcon.icon`.
- [ ] Add a cross-platform dynamic-icon compiler script.
- [ ] Wire the script into `beforeBundleCommand` and bundle `Assets.car`.
- [ ] Add the required macOS `Info.plist` icon declarations.
- [ ] Export all six renditions with `ictool` for visual verification.
- [ ] Build the macOS app bundle and inspect resources, plist, and signature.
- [ ] Run affected script/config checks and document compatibility behavior.
